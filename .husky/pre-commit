#!/bin/sh
if git diff --cached -U0 | grep -qiE '^\+.*(apikey|api_key|password|secret|token)[[:space:]]*[:=][[:space:]]*["'"'"'][A-Za-z0-9_-]{8,}'; then
  echo "BLOCKED: possible hardcoded secret in commit"
  exit 1
fi